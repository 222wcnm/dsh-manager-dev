# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

> 发布仓库只携带面向使用者的正式版本条目；开发期迭代流水账见开发仓库（不随发布物分发）。

## [0.3.0] - 2026-09-26

### 新增

- 项目显示名称改为 **Whalekeeper**；弹窗可点击会话行，直接打开对应的 Web UI 会话。
- 会话状态可由配套插件主动推送，后台提醒与弹窗会更快更新；连接中断时仍可回退到轮询。
- 新增全局安装、NPX、本地已构建源码三种启动方式。本地源码模式会临时跳过 profile 中的 `mcp-ssh`，不改写用户配置；其他方式保持原配置。
- 配套插件 0.4.0 可主动报告就绪状态和实际端口，宿主也保留原有探测方式。

### 修复

- 适配新版 dsh Web UI 的启动认证：打开托管实例时使用启动链接，外部实例仍受上游认证限制。
- 修复带认证令牌的会话深链拼接，避免令牌末尾被额外加入斜杠。
- 修复子代理活动状态、计划待审提醒，以及实时推送连接下的徽标更新。

### 已知限制

- macOS 与 Firefox 运行时仍未实测；Chrome Web Store 上架仍暂缓。

## [0.2.1] - 2026-08-25

### 文档

- **README 图文更新**：对齐当前 popup 现状——三视图截图（服务概览 / 会话状态感知 / 首选项设置）+ 深色主题图（均可由 `tools/ui-theme/capture-readme-shots.js` 复现）；功能清单、配置表与测试环境同步至当前版本（97 条 UI 断言 / 插件单测 33/33）；构建者信息补充

## [0.2.0] - 2026-08-25

### 新增

- **主题与深色模式**：popup / 日志页跟随 dsh Web UI 主题，四态可选（跟随 Web UI / 跟随系统 / 浅色 / 深色），与 Web UI 实时同步
- **工具栏徽标提醒「该点回来看看了」**：dsh 页面在后台时，会话状态变化点亮徽标——进行中蓝 n / 待确认黄「?」/ 工作完成绿「!」；图标角标=实例运行状态（绿点运行 / 红点异常 / 无点停止）
- **popup 会话状态感知**：只读会话摘要（不读消息内容）——待确认 > 进行中/已停止 > 已完成（新鲜显示）；已完成行可「已读」（3 秒撤销）；完成会话保留时长可设置（0 = 不显示已完成）；活跃子代理在父行下成树展示
- **Windows 隐藏控制台载体**：启动 / 重启 dsh 不再闪现终端窗口
- **颜色语义自定义**：设置「颜色角色」区可改 待确认/进行中/已完成 三色（预设色板点选即生效、一键恢复默认）；error 红与字符语义锁定，改红撞色有提示
- **popup 空间架构升级（侧边导轨）**：380×270 物理锁定、零抖动；官方原生视觉资产（小鲸鱼悬停游弋、[DSH WEB] 字标、进行中点阵）
- **dsh-lifecycle 插件会话端点升级**：`/_manager/sessions` 接入子代理感知（活动子代理运行状态折叠进父会话）

### 修复

- **徽标计数与 popup 会话区不一致**：计数数据源统一为插件服务端点（DOM 扫描仅回退），按端口归并去重
- **改端口后重启仍起旧端口**：重启以设置显式字段（host/port/profile）优先重放
- **完成提醒开关关闭瞬间未剔除既有提醒**（重开开关不再冒陈旧「工作完成」）；单次 set 改多键的时序竞态修复
- 日志查看页 / 页面内管理面板若干体验细节（日志页状态点不再被 2s 自动刷新干扰、扩展重载后旧面板提示刷新恢复）

### 已知限制

- macOS 与 Firefox 运行时仍未实测（无对应设备验证环境）
- 无 deep link 进入指定会话：popup 会话行为纯展示（Web UI 尚无 URL 会话定位），导航由「前往 Web UI 统一处理」承担
- 尚未上架 Chrome Web Store，需以「开发者模式」加载 `extension/` 目录

## [0.1.1] - 2026-08-16

### 新增

- **Windows 隐藏控制台载体**：启动 / 重启 dsh 不再闪现终端窗口（进程有控制台但从不显示）
- **`--port 0` 动态端口**：端口自动分配，启动后从日志回填实际端口并在 popup 展示；重启按动态端口语义重放
- **Linux 平台支持与安装器**：`install.sh` / `uninstall.sh`（用户级注册四份浏览器清单、无需 sudo）；Kali WSL2 实测通过；macOS 同源实现未实测
- **Firefox 宿主注册**：Chrome / Edge 之外增加 Mozilla 注册项（Firefox 运行时未实测）
- **日志查看页增强**：逐块「加载更早」、一键「复制全部」
- **外部实例发现生产路径实测**：终端手工启动的 dsh web 识别 / 接管 / 停止全链路

### 修复

- **页面内管理面板**：扩展重载 / 更新后，已打开页面里的旧面板不再永久红错「状态获取失败」——显示中性灰提示「扩展已重载或更新，请刷新页面恢复」（徽章「已断开」）并停止轮询；单次状态获取失败不再立即变红（连续 3 次约 6s 才显示错误态，期间保留上次成功状态）
- **圆点颜色改随展示语义**：绿=本页托管实例运行中、蓝=外部实例、灰=未托管/已停止、琥珀=启动/停止中、红=连续失败（此前「未托管」页面误显绿灯）
- **展开面板不再顶动胶囊**：胶囊位置固定，面板自胶囊上方弹出
- **徽标动态端口误探**：port 0 设置下徽标错误探测默认端口的问题（改经 native status 判定）；无监听端口时正确清空徽标
- **冒烟测试自足化**：全新克隆可直接跑全量冒烟（本地产物核对项自动跳过）

## [0.1.0] - 2026-08-14

首个公开版本：把 `dsh web` 的开关装进浏览器。

### 新增

- 一键启动 / 停止 / 重启 `dsh web`；停止自动走 `dsh-lifecycle` 优雅停机，插件缺失时强制降级
- 外部实例检测与接管：识别终端手工启动的 dsh web（任意端口，含 `--port 0`），「接管」后即可停止 / 重启
- 状态徽标与 popup 面板（stopped / starting / running / stopping / external），与 dsh Web UI 同款视觉
- 日志查看页：自动刷新尾部、逐块加载更早、一键复制全部
- dsh Web UI 页面内管理面板：右下角徽章，两步确认停止、一键重启
- 端口设为 `0` 自动分配动态端口，启动后自动回填实际端口
- 可选插件 `plugin/dsh-lifecycle`：`POST /_lifecycle/shutdown` 优雅停机 + `GET /_lifecycle/health` 健康端点
- 平台：Windows 10/11 + Chrome / Edge 生产实测；Linux 平台层与安装器（install.sh）实测通过（Kali WSL2）；Firefox 宿主注册就绪（运行时未实测）；macOS 已实现未实测

### 安全

- 仅 127.0.0.1 回环通信；不读取、不存储 dsh 凭据；宿主仅接受本扩展固定 ID

### 已知限制

- macOS 与 Firefox 运行时未实测（无对应设备验证环境）
- 未安装 `dsh-lifecycle` 时停止走强制终止，可能丢失最近数秒会话状态
- 尚未上架 Chrome Web Store，需以「开发者模式」加载 `extension/` 目录
